# Copy this to .env — OR just paste keys directly into the Settings panel in the UI.
# Keys set via the UI take precedence over environment variables.

ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...
GEMINI_API_KEY=AIza...
